# Welly AI Presentation Talk Track

ไฟล์นี้เป็นสคริปต์คำพูดสำหรับใช้พรีเซนต์โปรเจกต์ Welly AI

โครงแบ่งเป็น 3 ส่วนหลัก:
- RAG
- Frontend
- Backend

ในแต่ละหัวข้อจะมี:
- `พูดสั้น` สำหรับเวลาพรีเซนต์แบบกระชับ
- `ถ้าจะขยาย` สำหรับเวลาที่อาจารย์หรือกรรมการอยากฟังเพิ่ม

## เปิดพรีเซนต์

### ภาพรวมโปรเจกต์

พูดสั้น:
`โปรเจกต์ของเราคือ Welly AI เป็นผู้ช่วยด้านโภชนาการที่ใช้ระบบ RAG ร่วมกับ LLM เพื่อให้คำตอบอิงจากข้อมูลจริงใน dataset ของเรา ไม่ได้ตอบจากความรู้ทั่วไปของโมเดลอย่างเดียว`

ถ้าจะขยาย:
`แนวคิดหลักของโปรเจกต์นี้คือให้ระบบค้นข้อมูลที่เกี่ยวข้องจากฐานข้อมูลของเราก่อน แล้วค่อยส่งข้อมูลนั้นเข้า LLM เพื่อสร้างคำตอบ ทำให้คำตอบมี grounding กับ dataset มากขึ้น และลดโอกาส hallucination`

## ส่วนที่ 1: RAG

### จุดที่ 1: เราใช้ข้อมูลอะไรเป็นฐานความรู้

พูดสั้น:
`ในส่วน RAG เราเริ่มจากการรวบรวมข้อมูลจากหลายไฟล์ CSV เช่น ข้อมูลมาตรฐานโภชนาการ เกณฑ์ BMI กฎ DGA ข้อมูลสุขภาพผู้ใช้ และ dataset อาหารที่มี risk level`

ถ้าจะขยาย:
`เราไม่ได้ใช้แค่ไฟล์เดียว แต่รวมหลายตารางที่มีบทบาทต่างกัน เช่นบางตารางเป็น guideline บางตารางเป็นข้อมูลสุขภาพผู้ใช้ และบางตารางเป็นข้อมูลอาหารจริง จุดนี้ทำให้ระบบสามารถตอบได้หลายรูปแบบทั้งคำถามด้านมาตรฐานสุขภาพและคำถามเกี่ยวกับอาหาร`

### จุดที่ 2: เราแปลงข้อมูลตารางให้เป็นเอกสารยังไง

พูดสั้น:
`หลังจากโหลดข้อมูลแล้ว เราแปลงแต่ละแถวของตารางให้เป็นข้อความสั้น ๆ พร้อม metadata เช่นชื่อ table และ row index เพื่อให้ระบบ retrieval ค้นหาได้ง่ายขึ้น`

ถ้าจะขยาย:
`เพราะ vector search ทำงานกับข้อความ ไม่ได้ทำงานกับ DataFrame โดยตรง เราเลยออกแบบฟังก์ชันสำหรับแปลง row ของแต่ละตารางให้เป็นข้อความที่ยังรักษาความหมายเดิมไว้ เช่นตาราง BMI ก็จะแปลงเป็นข้อความแนวมาตรฐาน BMI ส่วนตารางอาหารก็จะแปลงเป็นข้อความที่มี calories, sugar, sodium และ risk level`

### จุดที่ 3: ทำไมต้อง chunking

พูดสั้น:
`หลังจากได้ documents แล้ว เรา split ข้อความเป็น chunks เพื่อให้ vector search ดึงส่วนที่เกี่ยวข้องที่สุดออกมาได้ดีขึ้น`

ถ้าจะขยาย:
`ในโปรเจกต์นี้ข้อมูลหลาย row ไม่ได้ยาวมาก แต่เราใส่ text splitting ไว้เพื่อให้ pipeline ของ RAG สมบูรณ์ และรองรับกรณีที่ข้อความยาวขึ้นในอนาคตด้วย`

### จุดที่ 4: embedding model ที่ใช้

พูดสั้น:
`สำหรับ embedding เราใช้โมเดล multilingual ชื่อ intfloat/multilingual-e5-small เพราะรองรับหลายภาษาและทำงานได้ดีบน CPU`

ถ้าจะขยาย:
`เหตุผลที่เลือกโมเดลนี้คือโปรเจกต์ของเรามีทั้งข้อความไทยและอังกฤษ โมเดลนี้จึงเหมาะกับการสร้าง vector representation ที่รองรับคำถามหลายภาษาโดยยังประหยัดทรัพยากรเครื่อง`

### จุดที่ 5: vector database ที่ใช้

พูดสั้น:
`เมื่อได้ embeddings แล้ว เราเก็บลง FAISS vector store เพื่อใช้ค้นหา context ที่ใกล้เคียงกับคำถามของผู้ใช้`

ถ้าจะขยาย:
`FAISS เป็นตัวช่วยให้เราทำ semantic search ได้เร็วขึ้น โดยเวลาผู้ใช้ถาม ระบบจะเปลี่ยนคำถามเป็น embedding แล้วค้นหา document chunks ที่มีความหมายใกล้เคียงที่สุดจาก vector database`

### จุดที่ 6: เราทดสอบ retrieval ยังไง

พูดสั้น:
`ก่อนจะต่อเข้า LLM เราทดสอบ retrieval แยกก่อน เพื่อดูว่าเมื่อถามคำถาม เช่น BMI หรือโซเดียม ระบบดึง document จากตารางที่ถูกต้องหรือไม่`

ถ้าจะขยาย:
`การแยกทดสอบ retrieval เป็นขั้นสำคัญ เพราะถ้า context ที่ดึงมาผิด ต่อให้ LLM ดีแค่ไหนก็มีโอกาสตอบผิด ดังนั้นเราจึงเช็ก top-k documents ก่อนว่าเกี่ยวข้องกับคำถามจริง`

### จุดที่ 7: prompt และ LLM

พูดสั้น:
`หลังจาก retrieve context ได้แล้ว เราจะสร้าง prompt ที่บังคับให้ LLM ใช้เฉพาะข้อมูลใน context และตอบเป็นภาษาไทยในโทนที่เหมาะกับผู้ช่วยด้านโภชนาการ`

ถ้าจะขยาย:
`ใน prompt เรากำหนดไว้ชัดว่าถ้าข้อมูลไม่พอ ระบบต้องบอกว่าข้อมูลไม่พอ และถ้าคำถามอยู่นอกขอบเขต เช่นอาหารสัตว์ ระบบต้องปฏิเสธ ไม่ใช่ตอบจากความรู้ภายนอกของโมเดลเอง`

### จุดที่ 8: flow หลักของการตอบคำถาม

พูดสั้น:
`flow หลักคือ ผู้ใช้ถาม ระบบตรวจ intent ก่อน จากนั้น retrieve context จาก vector store ที่เหมาะสม แล้วเอา context ไปสร้าง prompt และส่งเข้า LLM เพื่อสร้างคำตอบ`

ถ้าจะขยาย:
`ในฟังก์ชันหลัก ask_welly_rag เรามีทั้ง intent detection, out-of-scope checking, retrieval, context assembly, และ answer generation รวมอยู่ในจุดเดียว ทำให้ notebook นี้ทำงานเป็น chatbot prototype ได้จริง`

### จุดที่ 9: ทำไมเรามีหลาย vector store

พูดสั้น:
`นอกจาก knowledge store หลักแล้ว เรายังแยก recipe store และ calories store ออกมา เพื่อให้คำถามแต่ละประเภทไปค้นจาก corpus ที่เหมาะกับมันมากขึ้น`

ถ้าจะขยาย:
`ตัวอย่างเช่น ถ้าผู้ใช้ถามเรื่องสูตรอาหาร ระบบควรไปดู recipe corpus ก่อน แต่ถ้าถามเรื่องพลังงานหรือแคลอรี ก็ควรไปดู calories corpus ก่อน การแยก store แบบนี้ช่วยให้ retrieval มีคุณภาพดีขึ้น`

### จุดที่ 10: จุดเด่นของ RAG ในโปรเจกต์นี้

พูดสั้น:
`จุดเด่นคือระบบของเราไม่ได้ใช้ LLM ตอบลอย ๆ แต่ใช้ retrieval จาก dataset จริงก่อน ทำให้คำตอบมีที่มาชัดเจนและสามารถอ้าง source ได้`

ถ้าจะขยาย:
`อีกจุดที่สำคัญคือเรามี helper logic บางส่วนสำหรับคำถามเฉพาะทาง เช่นเรื่องคอเลสเตอรอลหรือน้ำตาล เพื่อช่วยให้คำตอบบางกรณีแม่นขึ้น ดังนั้นระบบนี้จึงเป็น RAG-driven system ที่มี rule-based support อยู่ด้วย`

### จุดที่ 11: ข้อจำกัดของ RAG ตอนนี้

พูดสั้น:
`ระบบนี้ยังเป็น prototype ดังนั้นบางคำถามที่อยู่นอก dataset หรือคำถามที่ต้อง reasoning ข้ามหลายแหล่งพร้อมกัน อาจยังมีข้อจำกัดอยู่`

ถ้าจะขยาย:
`โดยเฉพาะคำถามที่ต้อง aggregate ทั้ง dataset หรือคำถามที่ไม่ตรงกับ domain ของข้อมูล เราจึงออกแบบ fallback ไว้ เพื่อให้ระบบยอมบอกว่าข้อมูลไม่พอแทนที่จะตอบผิด`

## ส่วนที่ 2: Frontend

### จุดที่ 1: เทคโนโลยีฝั่งหน้าเว็บ

พูดสั้น:
`ฝั่ง frontend เราพัฒนาด้วย React และ Vite เพื่อให้ได้หน้าเว็บที่ตอบสนองเร็วและแยก component ชัดเจน`

ถ้าจะขยาย:
`เราเลือก React เพราะจัดการ component และ state ได้ง่าย เหมาะกับหน้า chatbot ที่ต้องมีการอัปเดตข้อความแบบต่อเนื่อง และใช้ Vite เพื่อให้การพัฒนาและ build เร็วขึ้น`

### จุดที่ 2: โครงหน้าหลักของเว็บ

พูดสั้น:
`หน้าเว็บแบ่งเป็น 3 หน้าหลัก คือ Home, Chat, และ History`

ถ้าจะขยาย:
`หน้า Home ใช้แนะนำระบบ หน้า Chat เป็นหน้าหลักที่เชื่อมกับ backend และหน้า History ใช้แสดงประวัติคำถามใน session ปัจจุบัน`

### จุดที่ 3: หน้า Home ทำหน้าที่อะไร

พูดสั้น:
`หน้า Home ทำหน้าที่เป็น landing page เพื่อบอกว่า Welly AI คืออะไรและชวนผู้ใช้เริ่มต้นใช้งาน`

ถ้าจะขยาย:
`เราออกแบบหน้า Home ให้มีข้อความแนะนำ จุดเด่นของระบบ และตัวอย่างเมนูสุขภาพ เพื่อให้ผู้ใช้เข้าใจ use case ของระบบก่อนเข้าไปยังหน้าแชตจริง`

### จุดที่ 4: หน้า Chat ทำงานยังไง

พูดสั้น:
`หน้า Chat เป็นหน้าหลักของระบบ โดยผู้ใช้พิมพ์คำถาม แล้ว frontend จะส่งคำถามไปที่ backend ผ่าน API และแสดงคำตอบกลับมาในรูปแบบ chatbot`

ถ้าจะขยาย:
`หน้า Chat จะมี state สำหรับเก็บข้อความทั้งหมดของบทสนทนา มี quick prompts สำหรับคำถามยอดนิยม และมี loading state เพื่อให้ผู้ใช้รู้ว่าระบบกำลังประมวลผลอยู่`

### จุดที่ 5: frontend รู้ได้ยังไงว่า backend พร้อม

พูดสั้น:
`เมื่อหน้า Chat ถูกเปิดขึ้นมา frontend จะเรียก endpoint /health เพื่อตรวจว่า backend พร้อมหรือยัง และเปิด LLM อยู่หรือไม่`

ถ้าจะขยาย:
`ผลจาก /health จะถูกนำมาแสดงเป็น badge เช่น LLM Ready หรือ Retrieval Mode ซึ่งช่วยให้ผู้ใช้และผู้พัฒนารู้สถานะของระบบทันทีจากหน้าเว็บ`

### จุดที่ 6: ตอนผู้ใช้ส่งคำถามเกิดอะไรขึ้น

พูดสั้น:
`เมื่อผู้ใช้กดส่ง frontend จะสร้าง user message ก่อน แล้วค่อยเรียก /api/chat จากนั้นเมื่อได้ response กลับมาก็จะสร้าง assistant message และแสดงแหล่งข้อมูลที่ใช้`

ถ้าจะขยาย:
`frontend ไม่ได้สร้างคำตอบเอง แต่เป็นเพียงตัวรับและแสดงผลจาก backend โดย response ที่ได้จะมีทั้ง answer, intent, sources, llm_enabled และ used_context_fallback`

### จุดที่ 7: ประวัติคำถามเก็บยังไง

พูดสั้น:
`ตอนนี้ history ถูกเก็บเป็น state ฝั่ง client เพื่อใช้แสดงประวัติในรอบการใช้งานปัจจุบัน`

ถ้าจะขยาย:
`นั่นหมายความว่าถ้ารีเฟรชหน้า ข้อมูลจะหาย เพราะยังไม่ได้เชื่อมต่อกับ database จริง จุดนี้เป็นขอบเขตของ prototype ฝั่ง frontend ในตอนนี้`

### จุดที่ 8: จุดเด่นของ frontend

พูดสั้น:
`จุดเด่นของ frontend คือ UI เรียบง่าย เข้าใจง่าย และเชื่อมกับ backend จริงแล้ว ไม่ได้เป็นแค่ mock interface`

ถ้าจะขยาย:
`ผู้ใช้สามารถเห็นทั้งสถานะระบบ คำตอบจาก backend และแหล่งข้อมูลที่ใช้ ซึ่งสอดคล้องกับแนวคิดของระบบ RAG ที่เน้นการตอบจาก context จริง`

## ส่วนที่ 3: Backend

### จุดที่ 1: backend ใช้เทคโนโลยีอะไร

พูดสั้น:
`ฝั่ง backend เราใช้ FastAPI เพราะเหมาะกับการสร้าง API ที่เบา เร็ว และมี schema ชัดเจน`

ถ้าจะขยาย:
`FastAPI ช่วยให้เราจัดการ request และ response ได้เป็นระบบ และเหมาะมากกับการเชื่อม frontend, RAG logic, และ LLM เข้าด้วยกัน`

### จุดที่ 2: โครง backend ถูกแบ่งยังไง

พูดสั้น:
`backend ของเราแยกออกเป็น 4 ส่วนหลัก คือ main.py, config.py, schemas.py และ service.py`

ถ้าจะขยาย:
`main.py ใช้เปิด API, config.py ใช้จัดการ environment และ path, schemas.py ใช้กำหนดรูปแบบข้อมูล, และ service.py เป็นแกนหลักของ logic ทั้งหมด`

### จุดที่ 3: startup ของ backend ทำอะไร

พูดสั้น:
`ตอน backend เริ่มทำงาน จะมีการ initialize service โดยโหลด data, embeddings, FAISS indexes และ LLM ไว้ตั้งแต่ตอน startup`

ถ้าจะขยาย:
`แนวทางนี้ทำให้ตอนรับ request จริง ระบบไม่ต้องโหลด model หรือ index ใหม่ทุกครั้ง จึงช่วยลด latency และทำให้ API ตอบได้เร็วขึ้น`

### จุดที่ 4: config ทำหน้าที่อะไร

พูดสั้น:
`config.py เป็นตัวรวม environment variables ทั้งหมด เช่น model name, API key, top-k, CORS และ path ของ embedding model`

ถ้าจะขยาย:
`เราเพิ่มความยืดหยุ่นให้ระบบโดยสามารถอ่าน .env ได้ทั้งจาก root project และจากโฟลเดอร์ backend รวมถึงรองรับการโหลด embedding model จาก local cache ด้วย`

### จุดที่ 5: schema มีประโยชน์ยังไง

พูดสั้น:
`schema ช่วยกำหนดรูปแบบ request และ response ของ API ให้ชัดเจน เช่น /api/chat ต้องรับ message และคืน answer พร้อม sources`

ถ้าจะขยาย:
`การมี schema ทำให้ frontend กับ backend คุยกันแบบชัดเจน ลดความผิดพลาดเรื่อง field name และช่วยให้ API maintenance ง่ายขึ้น`

### จุดที่ 6: WellyRAGService คืออะไร

พูดสั้น:
`WellyRAGService คือหัวใจของ backend เพราะรวม logic ตั้งแต่ load data, retrieve context, จนถึงเรียก LLM ไว้ใน class เดียว`

ถ้าจะขยาย:
`class นี้เป็นตัวเชื่อม notebook logic เดิมให้กลายเป็น production-style API service โดยแยก method สำหรับ loading, retrieval, cleaning, fallback และ direct handlers อย่างเป็นระบบ`

### จุดที่ 7: health endpoint ใช้ทำอะไร

พูดสั้น:
`endpoint /health ใช้บอกว่า backend พร้อมหรือยัง และตอนนี้ LLM เปิดอยู่ไหม`

ถ้าจะขยาย:
`frontend ใช้ข้อมูลนี้เพื่อแสดง badge บนหน้า chat และในมุมของการพัฒนา health endpoint ก็ช่วยให้ debug ได้ง่ายมาก เพราะเรารู้ทันทีว่า startup สำเร็จไหม`

### จุดที่ 8: /api/chat ทำงานยังไง

พูดสั้น:
`เมื่อ frontend ส่งคำถามเข้ามาที่ /api/chat backend จะเรียก rag_service.ask() แล้วส่งคำตอบที่ผ่าน retrieval และ generation กลับไป`

ถ้าจะขยาย:
`ภายใน ask() ระบบจะตรวจ out-of-scope ก่อน จากนั้นตรวจ direct handlers ถ้ายังไม่ตรงจะไปทำ intent detection, retrieval, prompt construction และ LLM invocation ต่อ`

### จุดที่ 9: direct handlers คืออะไร

พูดสั้น:
`direct handlers เป็น logic เสริมสำหรับคำถามเฉพาะทาง เช่นเรื่องโซเดียม น้ำตาล หรือคอเลสเตอรอล เพื่อช่วยให้บางคำถามตอบได้ตรงขึ้น`

ถ้าจะขยาย:
`นั่นคือเหตุผลที่ backend นี้ไม่ใช่ pure RAG ล้วน แต่เป็น hybrid pipeline ที่ใช้ RAG เป็นแกนหลักแล้วเสริม rule-based logic ในบางจุด`

### จุดที่ 10: fallback mode คืออะไร

พูดสั้น:
`ถ้า LLM ใช้ไม่ได้หรือ context ไม่เพียงพอ ระบบจะไม่ตอบมั่ว แต่จะใช้ fallback message หรือแสดงข้อมูลที่ retrieve ได้แทน`

ถ้าจะขยาย:
`แนวคิดนี้สำคัญมากสำหรับระบบสุขภาพ เพราะการยอมบอกว่าข้อมูลไม่พอ ปลอดภัยกว่าการเดาคำตอบที่ไม่มีหลักฐานรองรับ`

### จุดที่ 11: จุดเด่นของ backend

พูดสั้น:
`จุดเด่นของ backend คือแยกสถาปัตยกรรมชัด โหลด resource ตอน startup และสามารถทำงานได้ทั้งแบบมี LLM และแบบ retrieval fallback`

ถ้าจะขยาย:
`อีกจุดสำคัญคือมันสามารถ reuse logic จาก notebook เดิมได้ดี ทำให้จากเดิมที่เป็น prototype ใน notebook กลายเป็น API service ที่ frontend ใช้งานจริงได้`

## ปิดพรีเซนต์

### สรุปทั้งระบบ

พูดสั้น:
`สรุปแล้วระบบของเราประกอบด้วย frontend ที่รับคำถามจากผู้ใช้ backend ที่จัดการ API และ orchestration และส่วน RAG ที่ใช้ retrieval จาก dataset จริงก่อนส่งเข้า LLM เพื่อสร้างคำตอบที่ grounded มากขึ้น`

ถ้าจะขยาย:
`สิ่งที่เราอยากเน้นคือโปรเจกต์นี้ไม่ได้ให้ LLM ตอบลอย ๆ แต่ใช้โครงสร้างแบบ retrieve ก่อนตอบ ทำให้คำตอบมีแหล่งอ้างอิงจากข้อมูลจริงในระบบ และ frontend กับ backend ก็ถูกออกแบบให้เชื่อมกันได้ครบแบบ end-to-end`

## ถ้าจะพูดแบบ 1 นาที

`Welly AI เป็นผู้ช่วยด้านโภชนาการที่ใช้ RAG ร่วมกับ LLM โดยเริ่มจากการโหลดข้อมูลโภชนาการหลายชุด แปลงเป็น documents สร้าง embeddings และเก็บลง FAISS จากนั้นเมื่อผู้ใช้ถาม ระบบจะทำ retrieval เพื่อหา context ที่เกี่ยวข้องก่อน แล้วค่อยส่ง context เข้า LLM เพื่อสร้างคำตอบ ฝั่ง frontend ถูกพัฒนาด้วย React และ Vite สำหรับรับคำถามและแสดงผล ส่วน backend ใช้ FastAPI เพื่อเชื่อม frontend เข้ากับระบบ RAG และจัดการทั้ง health check, chat API, และ fallback logic ทำให้ระบบนี้ทำงานได้ครบแบบ end-to-end`

## ถ้าจะพูดแบบ 3 นาที

`โปรเจกต์ Welly AI ของเราเป็นระบบแนะนำด้านโภชนาการที่ใช้ Retrieval-Augmented Generation หรือ RAG เป็นแกนหลัก เริ่มจากการรวบรวมข้อมูลจากหลายตาราง เช่นมาตรฐานโภชนาการ กฎ DGA เกณฑ์ BMI ข้อมูลสุขภาพผู้ใช้ และข้อมูลอาหาร จากนั้นเราแปลงแต่ละแถวให้เป็นข้อความและสร้าง embeddings ด้วย multilingual model แล้วเก็บไว้ใน FAISS vector store เมื่อผู้ใช้ถาม ระบบจะวิเคราะห์ intent ก่อนว่าควรค้นจาก knowledge, recipe หรือ calories store แล้วดึง context ที่เกี่ยวข้องมาสร้าง prompt ก่อนส่งเข้า Groq LLM เพื่อสร้างคำตอบภาษาไทยที่อิงข้อมูลจาก dataset จริง ฝั่ง frontend ของเราใช้ React และ Vite โดยมีหน้า Home, Chat และ History ส่วนหน้า Chat จะเรียก backend ผ่าน /health และ /api/chat เพื่อเช็กสถานะระบบและรับคำตอบกลับมา ฝั่ง backend ใช้ FastAPI และมี WellyRAGService เป็นหัวใจหลักที่ดูแลตั้งแต่โหลดข้อมูล สร้าง vector stores ทำ retrieval และเรียก LLM จุดเด่นของระบบนี้คือไม่ได้ให้โมเดลตอบจากความจำเพียงอย่างเดียว แต่ตอบโดยอ้างอิงข้อมูลจริงในระบบ พร้อมมี fallback และ guardrails สำหรับกรณีข้อมูลไม่พอหรือคำถามนอกขอบเขต`
